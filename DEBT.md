# Debt

- `#9/W6` — `src/editor-command.ts · canCollectCurrentBlock` — thin wrapper over one comparison, kept only so the Reading-view availability rule stays testable without the `obsidian` runtime.
